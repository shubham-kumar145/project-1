#include<bits/stdc++.h>
using namespace std;
int main (){
    list<int>ls;
    ls.push_back(2);
    ls.emplace_back(34);
    ls.push_front(342);
    ls.emplace_front(54);
    //and other are same as vector
    return 0;
}